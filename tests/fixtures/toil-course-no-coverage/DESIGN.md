# Design

## Rendering pipeline {#pipeline}

A shader program is compiled from a vertex and a fragment shader and linked
before it can draw anything.

## Texture sampling policy {#sampling}

A sampler binds one texture unit to one uniform.
